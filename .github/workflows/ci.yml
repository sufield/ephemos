name: CI/CD Pipeline (MINIMAL)

# TEMPORARILY DISABLED - Only running Bazel CI workflow for now
# All original jobs moved to ci-original.yml
# Only Bazel CI (bazel-ci.yml) is active

on:
  # Minimal triggers to avoid conflicts with Bazel CI
  workflow_dispatch:  # Manual trigger only

env:
  GO_VERSION: '1.24.x'
  GOPROXY: https://proxy.golang.org,direct

jobs:
  placeholder:
    name: CI Jobs Disabled - Bazel CI Only
    runs-on: ubuntu-latest
    steps:
    - name: Notice
      run: |
        echo "🚫 All CI jobs are temporarily disabled"
        echo "🔧 Only Bazel CI workflow is active (bazel-ci.yml)"
        echo "📋 Original jobs saved in ci-original.yml"
        echo "🎯 Focus: Get Bazel build and test working first"