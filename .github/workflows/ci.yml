name: CI/CD Pipeline (DISABLED)

# ALL CI JOBS TEMPORARILY DISABLED
# Focus on lightweight HTTP over mTLS library without complex build systems

on:
  # Only manual trigger
  workflow_dispatch:

env:
  GO_VERSION: '1.24.x'
  GOPROXY: https://proxy.golang.org,direct

jobs:
  placeholder:
    name: CI Jobs Disabled
    runs-on: ubuntu-latest
    steps:
    - name: Notice
      run: |
        echo "🚫 All CI/CD jobs are temporarily disabled"
        echo "🎯 Focus: Lightweight HTTP over mTLS library development"
        echo "🔧 Using Make-based build system only"
        echo "📋 Bazel CI removed - library uses Go toolchain"