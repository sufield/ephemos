name: CI/CD Pipeline (DISABLED)

# TEMPORARILY DISABLED - Only running Bazel CI workflow for now
# This file contains all the original CI jobs that are disabled
# To re-enable: rename this file back to ci.yml and remove the Bazel CI workflow

env:
  GO_VERSION: '1.24.x'
  GOPROXY: https://proxy.golang.org,direct

on:
  # Disabled - no triggers
  workflow_dispatch:  # Only manual trigger

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  disabled-notice:
    name: CI Jobs Disabled
    runs-on: ubuntu-latest
    steps:
    - name: Notice
      run: |
        echo "🚫 All CI jobs are temporarily disabled"
        echo "📋 Only Bazel CI workflow is active"
        echo "🔧 Re-enable by renaming ci-disabled.yml to ci.yml"