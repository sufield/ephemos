1. Created structure must be in tree structure for readability
2. Does it make sense to have stave status command to help with their decisions
3. Why is there lot of .gitkeep files in different folders
4. stave doctor must check for git also and anything else that is required as prereqs
5. Make the output of stave template readable with easy copy paste commands on its own line
6. Fix validate, bad format error